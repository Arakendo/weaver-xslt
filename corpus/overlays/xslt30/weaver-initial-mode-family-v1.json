{
  "schemaVersion": 1,
  "suite": "W3C XSLT 3.0 test suite",
  "suiteRevision": "6f8fd9e966ae74a251a2604abef9d904c7bc5c9b",
  "profile": "weaver-initial-mode-family-v1",
  "requiredBackends": ["interpreter"],
  "selectionRationale": "Inventories the complete five-case initial-mode family after preserving requested mode names while retaining the local/tunnel parameter adapter gap separately.",
  "cases": [
    {
      "setFile": "tests/misc/initial-mode/_initial-mode-test-set.xml",
      "caseName": "initial-mode-001",
      "selection": "engine-unsupported",
      "rationale": "The harness preserves the requested mode; the interpreter currently rejects initial-mode execution with XTDE0040."
    },
    {
      "setFile": "tests/misc/initial-mode/_initial-mode-test-set.xml",
      "caseName": "initial-mode-002",
      "selection": "engine-unsupported",
      "rationale": "The harness preserves the requested mode; output declaration handling currently precedes the required XTDE0045 initial-mode diagnostic."
    },
    {
      "setFile": "tests/misc/initial-mode/_initial-mode-test-set.xml",
      "caseName": "initial-mode-003",
      "selection": "engine-unsupported",
      "rationale": "The harness preserves the requested mode; output declaration handling currently precedes the required XTDE0050 global-parameter diagnostic."
    },
    {
      "setFile": "tests/misc/initial-mode/_initial-mode-test-set.xml",
      "caseName": "initial-mode-004",
      "selection": "harness-unsupported",
      "rationale": "The harness currently cannot distinguish initial-mode local and tunnel parameters from global stylesheet parameters."
    },
    {
      "setFile": "tests/misc/initial-mode/_initial-mode-test-set.xml",
      "caseName": "initial-mode-005",
      "selection": "engine-unsupported",
      "rationale": "The harness preserves the requested name; the interpreter currently rejects initial-mode execution with XTDE0040."
    }
  ]
}
